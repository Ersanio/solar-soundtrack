/**
 * The scanner. Everything else the package offers — `edits`, `dialect`,
 * `echo-hazards`, `command-hazards`, `fir-override`, `commands/*` — is reached by
 * subpath, because each answers a different question about the tokens this
 * produces.
 */
export * from "./tokens";
