import { Component } from '@angular/core';

/**
 * The loop glyph on the transport's Loop toggle.
 *
 * ```html
 * <amk-icon-loop />
 * ```
 */
@Component({
  selector: 'amk-icon-loop',
  templateUrl: './icon-loop.html',
})
export class IconLoop {}
