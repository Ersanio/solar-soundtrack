# Reference sources, gitignored and not ours to format.
AddmusicKsrc/
AddmusicKreadme/

# Build output and caches
web/dist/
web/out-tsc/
web/.angular/
.selftest/

# Generated by `npm run build:worklet` and `npm run generate-git-info` — both are
# gitignored, and formatting them would only churn on the next build.
packages/spc/assets/player/spc-worklet.js
web/src/app/git-info.generated.ts

# Vendored: Blargg's snes_spc, built elsewhere. `web/public/` holds a mirror of
# both directories, written by `npm run sync-spc-assets`.
packages/spc/assets/player/
web/public/driver/
web/public/player/

# Generated by `ng ... --ai-config=claude` and safe to overwrite, so keep it
# byte-identical to what the CLI emits rather than to what Prettier prefers.
.claude/
web/.claude/

package-lock.json
